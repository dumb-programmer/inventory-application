extends layout 

block content 
    .centered
        form(action="#" method="POST" class="form confirmation-dialog")
            h1 Delete Item
            p Are you sure you want to delete this item? This action is not reverseable
            .btns-container 
                a(href=`/item/${itemId}`).secondary-btn Cancel 
                button(type="submit" class="danger-btn") Delete